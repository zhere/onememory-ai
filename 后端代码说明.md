# 后端代码说明

## 项目概述

这是一个基于 Node.js + Express.js 的智能记忆管理系统后端，支持本地开发和 Vercel 无服务器部署两种模式。系统集成了多种 AI 服务（OpenAI、Google AI、Anthropic 等），提供智能记忆存储、检索、向量搜索和 RAG（检索增强生成）功能。

## 技术栈

- **运行时**: Node.js 18+
- **Web 框架**: Express.js
- **编程语言**: TypeScript
- **数据库**: PostgreSQL (pg)
- **缓存**: Redis (ioredis)
- **向量数据库**: Pinecone
- **认证**: JWT + API Key
- **部署**: Vercel Functions
- **AI 集成**: OpenAI、Google AI、Anthropic

## 目录结构

### 核心后端目录

```
api_backup/
├── config/                 # 配置管理
│   └── index.ts           # 系统配置定义
├── middleware/            # Express 中间件
│   ├── auth.ts           # API 密钥认证
│   ├── errorHandler.ts   # 错误处理
│   └── rateLimit.ts     # 请求频率限制
├── services/             # 核心业务服务
│   ├── DatabaseService.ts      # 数据库操作
│   ├── EmbeddingService.ts     # 文本嵌入
│   ├── LLMAdapter.ts         # LLM 适配器
│   ├── MemoryManager.ts        # 记忆管理
│   ├── VectorDatabase.ts       # 向量数据库
│   ├── ProxyServer.ts          # 代理服务器
│   ├── RAGKnowledgeManager.ts  # RAG 知识管理
│   └── ZepGraphitiManager.ts   # Zep 图数据库
├── app.ts                # Express 应用配置
├── index.ts              # Vercel 部署入口
└── server.ts             # 本地开发服务器
```

### API 路由

```
src/routes/
├── auth.ts               # 认证相关
├── memories.ts          # 记忆管理
├── projects.ts          # 项目管理
├── config.ts            # 配置管理
├── chat.ts              # 聊天 API
├── enhanced-chat.ts     # 增强聊天
├── analytics.ts         # 分析统计
├── zep-graphiti.ts      # Zep 图数据库
└── rag-knowledge.ts     # RAG 知识源
```

### 数据库架构

```
migrations/
└── 001_initial_schema.sql  # PostgreSQL 初始架构
```

## 核心功能模块

### 1. 认证与授权

**文件**: `api_backup/middleware/auth.ts`
- API 密钥认证机制
- JWT Token 验证
- 用户权限管理

### 2. 记忆管理

**文件**: `api_backup/services/MemoryManager.ts`
- 记忆存储与检索
- 向量相似度搜索
- 记忆生命周期管理

### 3. 向量数据库

**文件**: `api_backup/services/VectorDatabase.ts`
- Pinecone 向量数据库集成
- 文本嵌入向量存储
- 相似度搜索算法

### 4. RAG 知识管理

**文件**: `api_backup/services/RAGKnowledgeManager.ts`
- 知识源管理
- 文档分块与索引
- 上下文检索优化

### 5. LLM 适配器

**文件**: `api_backup/services/LLMAdapter.ts`
- 多 LLM 提供商支持
- OpenAI、Google AI、Anthropic 集成
- 统一 API 接口

### 6. 代理服务器

**文件**: `api_backup/services/ProxyServer.ts`
- OpenAI 兼容 API
- 流式响应处理
- 请求转发与缓存

## API 端点

### 认证相关
- `POST /api/auth/*` - 用户认证

### 记忆管理
- `GET /api/v1/memories` - 获取记忆列表
- `POST /api/v1/memories` - 创建记忆
- `PUT /api/v1/memories/:id` - 更新记忆
- `DELETE /api/v1/memories/:id` - 删除记忆

### 项目管理
- `GET /api/v1/projects` - 获取项目列表
- `POST /api/v1/projects` - 创建项目
- `PUT /api/v1/projects/:id` - 更新项目
- `DELETE /api/v1/projects/:id` - 删除项目

### 聊天 API
- `POST /api/v1/chat/completions` - 聊天完成
- `POST /api/v1/enhanced-chat/*` - 增强聊天

### 配置管理
- `GET /api/v1/config/*` - 获取配置
- `PUT /api/v1/config/*` - 更新配置

### 分析统计
- `GET /api/v1/analytics/*` - 使用分析
- `GET /api/v1/tokens/*` - Token 使用统计

## 数据库架构

### 核心表结构

**users** - 用户账户
- 基本信息：邮箱、姓名、订阅计划
- 限制配置：API 密钥数量、项目数量、月度 Token 限制

**projects** - 用户项目
- 项目配置：代理 URL、分块配置、Token 配置
- 关联用户：外键关联到 users 表

**conversations** - 对话会话
- 会话管理：会话 ID、标题、元数据
- 项目关联：外键关联到 projects 表

**messages** - 消息记录
- 消息内容：角色、内容、Token 计数
- 对话关联：外键关联到 conversations 表

**api_keys** - API 密钥
- 密钥管理：密钥哈希、权限、速率限制
- 用户关联：外键关联到 users 表

**token_usage_logs** - Token 使用日志
- 使用跟踪：模型、输入/输出 Token、成本
- 用户和项目关联：外键关联

**usage_logs** - 通用使用日志
- 审计跟踪：操作、详情、IP 地址
- 用户和项目关联：外键关联

## 配置管理

### 环境变量配置

```env
# 数据库配置
DATABASE_URL=postgresql://user:password@localhost:5432/supermemory

# Redis 配置
REDIS_URL=redis://localhost:6379

# Pinecone 配置
PINECONE_API_KEY=your_pinecone_api_key
PINECONE_ENVIRONMENT=your_environment
PINECONE_INDEX_NAME=supermemory

# LLM 提供商配置
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key
GOOGLE_API_KEY=your_google_api_key

# 安全配置
JWT_SECRET=your_jwt_secret_key

# 其他配置
NODE_ENV=development
PORT=3001
```

### 系统配置

**文件**: `api_backup/config/index.ts`
- 数据库连接配置
- Redis 缓存配置
- Pinecone 向量数据库配置
- LLM 提供商配置
- 安全配置（JWT）
- 速率限制配置

## 部署模式

### 1. 本地开发模式

**入口文件**: `api_backup/server.ts`
- 启动本地 Express 服务器
- 端口：3001（可配置）
- 支持热重载开发

### 2. Vercel 无服务器模式

**入口文件**: `api_backup/index.ts`
- Serverless Functions 部署
- 自动扩缩容
- 按需计费

## 开发脚本

```json
{
  "scripts": {
    "server:dev": "nodemon",           // 启动后端开发服务器
    "client:dev": "vite",             // 启动前端开发服务器
    "dev": "concurrently \"npm run client:dev\" \"npm run server:dev\"", // 同时启动前后端
    "build": "vite build",            // 构建前端
    "vercel-build": "vite build --config vite.config.vercel.ts", // Vercel 构建
    "lint": "eslint .",               // 代码检查
    "check": "tsc --noEmit"            // TypeScript 类型检查
  }
}
```

## 安全特性

1. **API 密钥认证** - 所有 API 端点需要有效 API 密钥
2. **速率限制** - 防止 API 滥用
3. **CORS 配置** - 跨域请求控制
4. **Helmet** - HTTP 安全头
5. **输入验证** - 请求数据验证
6. **错误处理** - 统一的错误响应

## 性能优化

1. **Redis 缓存** - 响应缓存和会话存储
2. **数据库索引** - 优化的查询性能
3. **连接池** - 数据库连接管理
4. **流式响应** - 大响应的流式处理
5. **向量索引** - 高效的相似度搜索

## 监控与日志

1. **健康检查** - `/api/health` 端点
2. **使用统计** - Token 使用跟踪
3. **审计日志** - 用户操作记录
4. **错误日志** - 系统错误跟踪

这个后端系统为智能记忆管理应用提供了完整的服务端支持，包括用户管理、记忆存储、AI 集成、向量搜索等核心功能。