# OpenClaw 消耗 Token 过大的原因分析

## 一、架构级固有特性：Agent 循环机制

与传统大模型的一次性问答不同，OpenClaw 作为智能体（Agent），为了完成一个任务必须走**复杂的递归流程**：
任务拆解 → 搜索网页 → 阅读材料 → 发现信息不足 → 再次搜索 → 调用工具 → 信息反馈 → ...

在这个"智能体循环"中，Agent 与底层大模型的交互次数**从几十次到上百次不等**，单次复杂任务的推理成本可能直接飙到几十美元。这是 OpenClaw 被吐槽"烧钱"太快的根本原因。

---

## 二、工程实现缺陷：六大 Token 消耗黑洞

### 1. 上下文持续累积（占比 40-50%）
这是最大的"资金黑洞"。每次请求都会将整个对话历史、工具输出、技能定义等全部重新发送给模型：
- 基础系统提示本身就占 **15,000+ Token**（包含 23 个工具定义、技能描述等）
- 长期会话上下文轻松突破 **50,000 Token**，甚至达到 **10 万级**
- 用户反馈连续使用 3 天后，单次交互 Token 可达 10 万级，响应延迟飙升至分钟级

### 2. 工具输出无差别存储（占比 20-30%）
所有工具调用输出都会被永久存入会话记录。例如：
- 执行 `config.schema` 可能返回数万 Token 的 JSON
- 目录遍历命令会将整个文件列表存入历史
- 这些大输出在后续每次对话中都会被重复计费

### 3. 系统提示重复发送（占比 10-15%）
复杂的系统提示（5,000-10,000 Token）每次 API 调用都会重发。虽然 Anthropic 提供 Prompt Caching（缓存命中仅收 10% 费用），但缓存 TTL 只有 5 分钟，过期后需全额付费。

### 4. 技能全量加载机制
OpenClaw 默认加载所有可用技能（通常 15+ 个），每个技能会注入工具模式、系统提示扩展和内存检索钩子。即使未使用的技能，仍会持续占用 Token——实测加载 11 个基础技能，单轮对话即可产生 **15,000+ 额外 Token**。

### 5. 心跳与后台任务
心跳（Heartbeat）功能允许 AI 主动唤醒执行任务，但许多用户未意识到**每次心跳触发都是完整的 API 调用**，且携带完整会话上下文。有用户设置每 5 分钟检查邮件，结果一天仅心跳就烧掉 **$50**。

### 6. 模型选择不当
使用 Claude Opus（$15/M Token）处理"今天天气如何"这类简单任务，与使用 Haiku（$1/M Token）相比，成本相差 **25 倍**。

---

## 三、具体场景对比

| 场景 | OpenClaw Token 消耗 | 优化后消耗 |
|------|---------------------|------------|
| 日常桌面操作（打开软件、整理文件） | 8,000-12,000 | **0**（本地执行） |
| 10 页 PDF 摘要 | 35,000 | **2,000**（本地解析后仅上传核心内容） |
| 200 轮长期会话 | 平均单轮 600 Token（总计 12 万上下文） | 平均单轮 **40 Token**（上下文稳定在 8,000） |
| 72 小时静默运行 | 约 15 万 Token（心跳+后台任务） | **0**（本地待机） |

---

## 四、优化建议

1. **定期重置会话**：避免上下文无限膨胀
2. **智能模型路由**：简单任务用 Haiku/Flash，复杂任务才用 Sonnet/Opus
3. **禁用不必要技能**：只加载当前任务需要的技能
4. **调整心跳频率**：避免过于频繁的自动触发
5. **使用本地模型**：通过 LM Studio 等部署本地模型处理简单任务
6. **采用记忆外挂**：如 MemOS 插件可降低 **60%** Token 消耗

---

## 五、总结

OpenClaw 的高 Token 消耗是**架构级特性**（Agent 需要多轮推理）与**工程缺陷**（全量上下文注入、无差别技能加载、缺乏本地执行能力）共同导致的结果。

对于追求成本可控的场景，可考虑采用"大模型+MCP"架构的替代方案（如 Keyvox），通过本地优先执行将 Token 消耗降至 **1/10-1/20**。

---
