# Onememory 系统使用说明

## Onememory 系统核心思想：以“项目”为中心

Onememory 的核心思想是构建一个可扩展、可定制的“第二大脑”或知识库。它不仅仅是一个简单的数据存储系统，更是一个能够帮助您整合、理解、并利用信息的智能平台。

系统的关键设计在于 **以“项目（Project）”为中心**。您可以把每个“项目”想象成一个独立的工作区或一个专门针对特定主题的“大脑”。您在项目中进行的所有操作，从数据汇集到知识图谱的构建，都归属于该项目，并与其他项目完全隔离。

这种设计确保了不同知识体系的独立性和专注性，让您可以在“AI发展史”和“市场调研”等不同项目间无缝切换，而不会造成信息混淆。

## 系统使用流程详解

一个典型的 Onememory 使用流程遵循以项目为核心的原则：

### 步骤一：选择或创建项目 (`Projects.vue`) - **一切的起点**

*   **入口:** `src/pages/Projects.vue`
*   **核心作用:** 这是您使用系统的第一步。在这里，您可以创建一个新项目（如“个人学习”），或从现有项目列表中选择一个进入。**您之后的所有操作都将在您选定的项目上下文中进行。**

### 步骤二：在项目内汇集与构建知识

选定项目后，您就可以开始为这个“大脑”填充内容并进行配置。以下所有功能模块的操作和数据都与您当前选择的项目**强关联**。

#### 1. 汇集记忆 (Project-Specific)
*   **手动添加 (`Memory.vue`):** 在当前项目中手动创建和编辑“记忆”。
*   **连接知识源 (`RAGKnowledgeSources.vue`):** 为当前项目添加外部知识源（如PDF、网页），这些知识源仅供本项目使用。

#### 2. 配置处理规则 (Project-Specific)
*   **文本分段 (`SegmentationConfig.vue`):** 为当前项目配置专属的文本分段策略。
*   **关系推断 (`RelationInference.vue`):** 查看和管理在当前项目内推断出的实体关系。
*   **记忆合成 (`MemorySynthesis.vue`):** 在当前项目中，根据已有记忆合成新的知识。

### 步骤三：在项目内探索与利用知识

当您的项目知识库初具规模后，就可以开始探索和利用这些专属于该项目的知识了。

*   **知识图谱 (`KnowledgeGraph.vue`):** 可视化展示**当前项目**的知识图谱，探索其内部的数据关联。
*   **融合搜索 (`FusionSearchConfig.vue`):** 在当前项目中，配置并使用搜索功能，精准查找信息。
*   **时序分析 (`TemporalEntities.vue`):** 分析当前项目中具有时间戳的数据。
*   **分析与洞察 (`Analytics.vue`):** 从宏观上分析当前项目的知识库统计数据。

### 步骤四：全局系统管理

与项目功能不同，以下功能是**全局配置（Global）**，它们的设置会影响整个应用程序，而不是单个项目。

*   **API 文档 (`ApiDocs.vue`):** 提供整个系统所有可用 API 的文档。
*   **代理配置 (`ProxyConfig.vue`):** 配置整个应用的全局网络代理，用于访问外部资源。
*   **Token 管理 (`TokenManagement.vue`):** 管理全局的 API 访问令牌。
*   **系统设置 (`Settings.vue`):** 进行全局的外观主题等偏好设置。

## 功能串联使用示例

假设您是一位需要进行“人工智能发展史”研究的学生。您可以这样使用 Onememory：

1.  **创建项目:** 在 `Projects.vue` 创建一个名为“AI 发展史研究”的项目，并**进入该项目**。
2.  **汇集资料:**
    *   在 `RAGKnowledgeSources.vue` 中，导入您收集的关于 AI 的 PDF 论文、网页文章和书籍。这些资料将**只属于**“AI 发展史研究”项目。
    *   在 `Memory.vue` 中，手动记录您自己的思考和课堂笔记。
3.  **知识构建:** 系统会自动处理这些资料，进行文本分段、实体识别和关系推断，构建专属于此项目的知识体系。
4.  **探索与学习:**
    *   打开 `KnowledgeGraph.vue`，您会看到一张关于 AI 发展史的知识图谱。
    *   当您想了解“强化学习”时，使用搜索功能，系统会**仅在本项目**的知识库中进行查找。
5.  **切换项目:** 如果您想转而研究“市场营销”，只需返回 `Projects.vue`，创建或选择“市场营销”项目，即可进入一个全新的、干净的工作区。

## 总结

Onememory 通过以**项目为核心**的设计，将数据管理、知识构建和智能分析功能模块化地整合在一起。它为您提供了一个既能宏观管理多个知识领域，又能微观深入探索单个领域内部联系的强大平台。
